const LICENSE_HASH = "b10b7769dfe6c5eaa5862ea22bee59a81a081ca97a0a7d3bee195f4e541f4428"; let executors = []; let games = []; let scripts = []; let monacoEditor; require.config({ paths: { 'vs': 'https://cdnjs.cloudflare.com/ajax/libs/monaco-editor/0.39.0/min/vs' } }); require(['vs/editor/editor.main'], function () { monacoEditor = monaco.editor.create(document.getElementById('monaco-editor'), { value: "-- Main script content will load here", language: 'lua', theme: 'vs-dark', automaticLayout: true, minimap: { enabled: false } }); const mainScript = scripts.find(s => s.name === 'CSLoader.lua'); if (mainScript) monacoEditor.setValue(mainScript.content); }); const socket = io(); socket.on('initial_data', (data) => { executors = data.executors || []; games = data.games || []; scripts = data.scripts || []; if (monacoEditor) { const mainScript = scripts.find(s => s.name === 'CSLoader.lua'); if (mainScript) monacoEditor.setValue(mainScript.content); } }); socket.on('data_updated', (data) => { executors = data.executors || []; games = data.games || []; scripts = data.scripts || []; if (adminDashboard.classList.contains('active')) { _0x6a(); } }); const loginView = document.getElementById('login-view'); const adminDashboard = document.getElementById('admin-dashboard'); const formLogin = document.getElementById('form-login'); const licenseInput = document.getElementById('license-key'); const loginError = document.getElementById('login-error'); const btnLogout = document.getElementById('btn-logout'); const adminExecutorsList = document.getElementById('admin-executors-list'); const adminGamesList = document.getElementById('admin-games-list'); const formAddExecutor = document.getElementById('form-add-executor'); const formAddGame = document.getElementById('form-add-game'); const modalOverlay = document.getElementById('modal-overlay'); const modalMessage = document.getElementById('modal-message'); const modalConfirmBtn = document.getElementById('modal-confirm'); const modalCancelBtn = document.getElementById('modal-cancel'); async function sha256(message) { const msgBuffer = new TextEncoder().encode(message); const hashBuffer = await crypto.subtle.digest('SHA-256', msgBuffer); const hashArray = Array.from(new Uint8Array(hashBuffer)); return hashArray.map(b => b.toString(16).padStart(2, '0')).join(''); } formLogin.addEventListener('submit', async (e) => { e.preventDefault(); const key = licenseInput.value.trim(); if (key === LICENSE_HASH) { alert("Security Error: Direct hash input detected."); return; } try { const hashedKey = await sha256(key); if (hashedKey === LICENSE_HASH) { setTimeout(() => { showDashboard(); }, 50); } else { alert("Invalid license key. Please check the spelling."); loginError.textContent = "Invalid license key. Please try again."; loginError.style.display = 'block'; licenseInput.value = ''; } } catch (err) { alert("Critical Error during login: " + err.message); } }); btnLogout.addEventListener('click', () => { location.reload(); }); function checkAuth() { loginView.style.display = 'flex'; adminDashboard.classList.remove('active'); } function showDashboard() { loginView.style.display = 'none'; adminDashboard.classList.add('active'); _0x6a(); } function _0x6a() { renderAdminExecutors(); renderAdminGames(); renderAdminScripts(); } function renderAdminExecutors() { adminExecutorsList.innerHTML = ''; if (executors.length === 0) { adminExecutorsList.innerHTML = '<div class="empty-state">No executors configured yet.</div>'; return; } executors.forEach(exe => { const item = document.createElement('div'); item.className = 'admin-list-item'; const statusClass = exe.status === 'Online' ? 'text-green' : ''; item.innerHTML = ` <div class="admin-list-info"> <p>${exe.name}</p> <span class="${statusClass}">Status: ${exe.status}</span> </div> <button class="btn-delete" onclick="window.deleteExecutor(${exe.id})" title="Delete"> <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"> <polyline points="3 6 5 6 21 6"></polyline> <path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2"></path> </svg> </button> `; adminExecutorsList.appendChild(item); }); } function renderAdminGames() { adminGamesList.innerHTML = ''; if (games.length === 0) { adminGamesList.innerHTML = '<div class="empty-state">No supported games added yet.</div>'; return; } games.forEach(game => { const item = document.createElement('div'); item.className = 'admin-list-item'; item.innerHTML = ` <div class="admin-list-info"> <p>${game.title}</p> <a href="${game.link}" target="_blank">🔗 View Link</a> </div> <button class="btn-delete" onclick="window.deleteGame(${game.id})" title="Delete"> <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"> <polyline points="3 6 5 6 21 6"></polyline> <path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2"></path> </svg> </button> `; adminGamesList.appendChild(item); }); } function renderAdminScripts() { const list = document.getElementById('admin-scripts-list'); if (!list) return; list.innerHTML = ''; const gameScripts = scripts.filter(s => s.name !== 'CSLoader.lua'); if (gameScripts.length === 0) { list.innerHTML = '<div class="empty-state">No game scripts added yet.</div>'; return; } gameScripts.forEach(script => { const item = document.createElement('div'); item.className = 'admin-list-item'; item.innerHTML = ` <div class="admin-list-info"> <p style="margin-bottom:4px;"><strong>${script.name}</strong></p> <div class="code-box" style="margin-top: 4px; padding: 6px 10px;"> <code style="font-size:11px;">loadstring(game:HttpGet("http://${window.location.host}/scripts/${script.name}"))()</code> </div> </div> <button class="btn-delete" onclick="window.deleteScript('${script.name}')" title="Delete"> <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"> <polyline points="3 6 5 6 21 6"></polyline> <path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2"></path> </svg> </button> `; list.appendChild(item); }); } function showModal(message) { return new Promise((resolve) => { modalMessage.textContent = message; modalOverlay.classList.add('active'); const cleanup = (value) => { modalOverlay.classList.remove('active'); modalConfirmBtn.onclick = null; modalCancelBtn.onclick = null; resolve(value); }; modalConfirmBtn.onclick = () => cleanup(true); modalCancelBtn.onclick = () => cleanup(false); modalOverlay.onclick = (e) => { if (e.target === modalOverlay) cleanup(false); }; }); } window.deleteExecutor = async function (id) { const confirmed = await showModal('Are you sure you want to delete this executor?'); if (confirmed) { executors = executors.filter(e => e.id !== id); saveAndRender(); } }; window.deleteGame = async function (id) { const confirmed = await showModal('Are you sure you want to delete this game?'); if (confirmed) { games = games.filter(g => g.id !== id); saveAndRender(); } }; window.deleteScript = async function (name) { const confirmed = await showModal('Are you sure you want to delete this script?'); if (confirmed) { try { await fetch('/api/admin/delete-script', { method: 'POST', headers: { 'Content-Type': 'application/json', 'Authorization': LICENSE_HASH }, body: JSON.stringify({ scriptName: name }) }); } catch (err) { } } }; async function saveAndRender() { try { const response = await fetch('/api/admin/save', { method: 'POST', headers: { 'Content-Type': 'application/json', 'Authorization': LICENSE_HASH }, body: JSON.stringify({ executors, games }) }); if (response.ok) { _0x6a(); } } catch (err) { } } formAddExecutor.addEventListener('submit', (e) => { e.preventDefault(); const name = document.getElementById('exe-name').value; const status = document.getElementById('exe-status').value; const unc = document.getElementById('exe-unc').value; const sunc = document.getElementById('exe-sunc').value; const type = document.getElementById('exe-type').value; const newExe = { id: Date.now(), name, status, unc, sunc, type }; executors.push(newExe); saveAndRender(); formAddExecutor.reset(); }); formAddGame.addEventListener('submit', (e) => { e.preventDefault(); const title = document.getElementById('game-title').value; const logo = document.getElementById('game-logo').value; const link = document.getElementById('game-link').value; const description = document.getElementById('game-desc').value; const newGame = { id: Date.now(), title, logo, link, description }; games.push(newGame); saveAndRender(); formAddGame.reset(); }); const btnSaveMainScript = document.getElementById('btn-save-main-script'); if (btnSaveMainScript) { btnSaveMainScript.addEventListener('click', async () => { if (!monacoEditor) return; const scriptContent = monacoEditor.getValue(); btnSaveMainScript.disabled = true; btnSaveMainScript.innerText = 'Saving...'; try { const response = await fetch('/api/admin/add-script', { method: 'POST', headers: { 'Content-Type': 'application/json', 'Authorization': LICENSE_HASH }, body: JSON.stringify({ scriptName: 'CSLoader.lua', scriptContent }) }); const data = await response.json(); if (data.success) { document.getElementById('main-script-result').style.display = 'block'; document.getElementById('main-loadstring-output').innerText = data.loadstring; alert('Main script saved and pushed explicitly!'); } } catch (err) { alert('Error pushing main script.'); } finally { btnSaveMainScript.disabled = false; btnSaveMainScript.innerText = 'Save Main Script & Push to GitHub'; } }); } const formAddGameScript = document.getElementById('form-add-game-script'); if (formAddGameScript) { formAddGameScript.addEventListener('submit', async (e) => { e.preventDefault(); let scriptName = document.getElementById('game-script-name').value.trim(); const scriptContent = document.getElementById('game-script-content').value; const btn = document.getElementById('btn-add-game-script'); btn.disabled = true; btn.innerText = 'Processing...'; try { const response = await fetch('/api/admin/add-script', { method: 'POST', headers: { 'Content-Type': 'application/json', 'Authorization': LICENSE_HASH }, body: JSON.stringify({ scriptName, scriptContent }) }); const data = await response.json(); if (data.success) { alert('Game loadstring saved and pushed to GitHub!'); formAddGameScript.reset(); } } catch (err) { alert('Error pushing game script.'); } finally { btn.disabled = false; btn.innerText = 'Create Game Loadstring'; } }); } checkAuth(); function _0x8a() { const container = document.getElementById('particles'); if (!container) return; const particleCount = 15; for (let i = 0; i < particleCount; i++) { const particle = document.createElement('div'); particle.className = 'particle'; const size = Math.random() * 4 + 2; const duration = Math.random() * 20 + 10; const opacity = Math.random() * 0.4 + 0.1; const drift = (Math.random() - 0.5) * 200; particle.style.width = `${size}px`; particle.style.height = `${size}px`; particle.style.left = `${Math.random() * 100}%`; particle.style.setProperty('--duration', `${duration}s`); particle.style.setProperty('--opacity', opacity); particle.style.setProperty('--drift', `${drift}px`); particle.style.animationDelay = `${Math.random() * -20}s`; container.appendChild(particle); } } function _0xba() { _0x8a(); } if (document.readyState === 'loading') { document.addEventListener('DOMContentLoaded', _0xba); } else { _0xba(); };window['renderAll'] = _0x6a;window['startAdmin'] = _0xba;window['deleteExecutor'] = deleteExecutor;window['deleteGame'] = deleteGame;window['sha256'] = sha256;